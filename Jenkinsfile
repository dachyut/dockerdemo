pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                //git 'https://github.com/dachyut/dockerdemo'
                sh 'echo myCustVar = myCustEnvVar'
            }
        }
    }
}